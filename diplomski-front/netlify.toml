[build]
  command = "npm run build -- --configuration production"
  publish = "dist/diplomski-front"

[build.environment]
  NODE_VERSION = "14"
  NPM_FLAGS = "--legacy-peer-deps"

# Angular client-side routes (refresh / deep links)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
